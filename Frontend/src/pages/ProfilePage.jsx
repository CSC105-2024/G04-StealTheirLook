import {Link, NavLink} from "react-router-dom";

const ProfilePage = () => {
    return(
        <div>
            <NavLink to="/Login">Logout</NavLink>
        </div>
    );
}
export default ProfilePage;